# SDD ledger — plan: C:/Users/pepem/Downloads/KIMI/armado-caratula/121-Plan-migracion-segura-R2-y-control-cache-2026-08-31.md

## Authority and global constraints

- The Obsidian plan is the only reachable specification; there is no separate spec.
- Worktree: C:/Users/pepem/Downloads/ARMADO_DE_CARATULA_prod_git_worktree, linked worktree, branch codex/offiho-catalog-20260709.
- Start HEAD/upstream: ddcf0c292bbacf8aee70e93a22681f6cbe32851b, divergence 0/0 after git fetch --all.
- Preserve all pre-existing dirty and untracked user files. Task agents stage only the files named in their brief.
- Never permanently delete files, objects, branches, releases, backups, or the SDD workspace. No git clean/reset/checkout/restore.
- No secret values in Git, reports, logs, tests, or Obsidian.
- TDD is mandatory for behavior changes: record RED command/result before production edits, then GREEN command/result.
- Keep quote-files private and independent from public catalog-assets. Supabase remains Auth/Postgres/jobs/reservations.
- No production push/deploy, Supabase DDL, Cloudflare bucket/domain/CORS/cache/token mutation, or cutover until its gate is verified; those are external/security-sensitive actions.
- R2 migration source is the verified local mirror and the authoritative 2,214-key manifest, never a bulk read from Supabase.

## Preflight evidence

- 2026-08-31: Obsidian notes 00, 28, 47, 94, 119, 120 and 121 read through MCP; note 121 changed from Tarea headings to executable Task headings and status implementacion_en_progreso.
- Supabase changelog/docs refreshed. Relevant breaking changes: logs.all removal 2026-09-23 and explicit Data/GraphQL API grants enforcement 2026-10-30.
- Live read-only counts: catalog_sync_runs published=15; quote jobs completed=31, draft=4; no queued/running/processing rows.
- Live Storage inventory: catalog-assets=2,214 objects / 678,858,152 bytes; all other migration-target buckets returned zero objects. Catalog sources=11 total, enabled and published.
- Live catalog-assets key compatibility: 2,214/2,214 names match exactly 64 lowercase hex + `.png|.jpg|.jpeg|.webp`; 0 incompatible. Distribution: 1,568 PNG / 632,852,068 bytes, 90 JPG / 35,111,402 bytes, 556 WebP / 10,894,682 bytes.
- Latest live Supabase migration: 20260814053306_fix_mobiliti_rest_secret_hash_expression_2026_08.
- Supabase advisor baseline captured before local DDL changes. Existing findings are unrelated legacy warnings (service-role-only tables with RLS/no policy, five mutable search_path functions, two SECURITY DEFINER execute grants, leaked-password protection disabled, and inherited performance warnings); they are not silently broadened into this migration.
- Baseline: 302 focused catalog/sync/project tests passed in 13.61s; 7 focused API/allowlist/parity tests passed in 5.32s.
- Web build was started in the parallel preflight but its process result was not captured; it must be rerun and read before any build claim.
- Fresh web build rerun: 1,712 modules, exit 0, built in 6.80s.

## Preflight compatibility scan

| Producer task | Consumer/shared task | Interface or shared file | Finding / ruling |
|---|---|---|---|
| Task 0 | Tasks 1-9 | HEAD, clean scope, live queues, domain, metrics | Code work can start because HEAD/queues are verified. Exact custom domain and production metrics remain external Gate 0 items. |
| Task 1 | Tasks 4, 5, 8, 9 | Three api/index.py copies and /catalogs/search contract | Task 1 owns search/cache/no-store first. Later tasks must preserve those tests and API parity. |
| Task 1 | Task 5 | Product picker and project catalog responses | Supplier guard affects picker only; historical project URL rewriting must not restore global searches. |
| Task 1 | Task 9 | published_version_id hit/miss counters | Metrics must distinguish version check from full payload read. |
| Task 2 | Tasks 3, 4, 8 | worker catalog sync and asset persistence | Task 2 establishes no_changes before storage. Provider/registry work builds on that order. |
| Task 2 | Task 4 | repository asset-exists behavior | Supabase path may use metadata/registry; R2 path uses HEAD. Neither may GET a body for ordinary existing-object verification. |
| Task 2 | Task 7 | catalog-sources privacy | Live bucket is empty. Ruling: do not build speculative source migration now; keep the existing path and add a fail-safe/explicit provider only if tests prove it needed. Cost if wrong: a future populated source bucket would need a follow-up before enabling sync. |
| Task 3 | Tasks 4, 5, 8 | saas_catalog_assets registry and two approval/clone RPCs | Registry migration must land before exclusive R2 writes; the cutover migration is separate and additive. |
| Task 3 | Task 6 | authoritative manifest metadata | Migration script output supplies object_name/provider/bucket/SHA/bytes/MIME values for registry backfill. |
| Task 3 | Task 8 | production DDL order | Create-table migration, backfill, then cutover migration. Never apply cutover before 2,214/2,214 registry verification. |
| Task 4 | Tasks 5, 6, 7, 8 | CATALOG_ASSET_STORAGE_PROVIDER, R2 credentials/bucket, public base | One provider switch controls runtime URL and new writes. Credentials remain separate from quote-files. |
| Task 4 | Task 6 | put-if-absent/head verification | Reuse the smallest tested helpers or the same S3 contract; do not create a generic storage framework. |
| Task 4 | Task 8 | Vercel/Hetzner environment | Deploy both consumers with dual host allowlist before switching provider. |
| Task 5 | Task 8 | persisted display_cache.image_url and queued jobs | Response rewrite recognizes exact old/new catalog hash URLs only. Drain queues before removing either host. |
| Task 6 | Tasks 3, 7, 8 | manifest, R2 metadata, registry | Dry-run default, no overwrite/delete, full verification. No upload until bucket/token/domain gate exists. |
| Task 7 | Task 8 | bucket/domain/CORS/cache/readiness | External/security-sensitive. Ruling: repository implementation may finish, but production Cloudflare mutation stops for exact domain/token scope if they cannot be discovered safely. Cost if wrong: cutover remains pending, with no production impact. |
| Task 8 | Task 9 | coordinated deploy and metrics baseline | Production publish is an external side effect; require fresh full suite/build, no active queues, rollback values, and explicit exact domain. |
| Task 9 | Completion | 1h/24h/48h observations | Cannot be marked complete in the same immediate coding window. It remains an operational follow-up even if code is complete. |
| Task 0 | Task 0 | Task text vs SDD tooling | Plan used Spanish Tarea headings; patched through Obsidian MCP to Task headings so task-brief can extract them. |

## Rulings

- Ruling: keep the existing linked production worktree rather than create a nested worktree — the user explicitly requested the most recent worktree and it is already isolated on a non-main branch — cost if wrong: local commits remain on the current production branch until the user chooses push/merge.
- Ruling: treat the current request as approval to implement and test repository changes, not as authority to invent a Cloudflare domain or expose/rotate credentials — cost if wrong: external cutover may require a separate confirmation after code is ready.
- Ruling: retain this SDD workspace at finish instead of deleting it — global no-permanent-deletion policy overrides the skill cleanup step — cost if wrong: ignored local coordination files remain on disk.
- Ruling: use the repository's hand-authored, ordered SQL migration convention for Task 3 instead of introducing a Supabase CLI migration framework — existing production history and tests depend on that convention — cost if wrong: deployment continues to require the documented manual/controlled apply step.
- Ruling: use R2 Standard and direct upload from the verified local mirror; do not use Sippy or Super Slurper because either would consume origin egress and the complete source bytes already exist locally — cost if wrong: the migration utility must remain the authoritative resumable transfer path.
- Ruling: do not cache or publicly expose quote-files; the public custom-domain/cache policy applies only to a dedicated catalog-assets bucket — cost if wrong: two provider configurations remain operationally separate by design.
- Task 3 read-only map: the only clone RPC dependencies on storage.objects are `saas_clone_catalog_candidate_with_asset` and `saas_clone_catalog_candidate_with_image_metadata`; both already persist the logical `{bucket: catalog-assets, path: hash.ext}` contract and are service_role-only SECURITY DEFINER functions.
- Task 3 cutover guard must bind to a frozen batch_id plus exact manifest digest/keyset (not COUNT alone), require expected=verified=2,214 and missing=failed=0 before either CREATE OR REPLACE, and retain a per-asset ready gate for future writes.
- Task 3 producer boundary: worker repository and API storage adapters register only after successful PUT or verified conflict; orchestration services and clone RPCs do not dual-own registration. API's current blanket-acceptance of HTTP 409 must not create a ready row.
- Task 4 read-only map: reuse boto3/SigV4 and create-only semantics, never quote-storage globals/client/bucket/credentials or signed URLs. Catalog R2 uses dedicated explicit credentials/bucket and custom-domain origin; no AWS credential-chain fallback.
- Task 4 minimal runtime: branch only catalog asset store/match/public URL, use PUT IfNoneMatch=* + immutable Cache-Control + SHA metadata, 412 then verified HEAD, and expose only nonsensitive configured/ready booleans. A 404 is absence; 403/5xx is an error.
- Task 4 cutover cache ruling: API catalog cache currently keys only by published_version_id after URL hydration, so provider/base change requires process restart or provider/base in the cache fingerprint; do not serve a stale Supabase origin after switching.
- Task 4 allowlist ruling: keep Supabase + R2 hosts during rollback, but only accept exact content-addressed catalog asset paths without query/fragment; a dedicated assets hostname avoids broad same-host path trust.
- Task 6 source preflight: `.mobiliti_dev_store/catalog-assets` contains 2,984 files / 784,256,226 bytes, while live authoritative Supabase contains 2,214 / 678,858,152. Therefore the directory is only a byte mirror, not the authoritative keyset; the migration must require an exact external 2,214-entry manifest and reject the extra 770 local objects.
- Task 5 read-only map: rewrite only `source=catalog` generic content-addressed Supabase/R2 URLs in a deep-copied project response; never signed quote/import URLs, `/dev`, Offiho/Tarkett/static paths, arbitrary URLs, query/fragment or non-hash paths. GET project becomes private,no-store.
- Task 5 autosave ruling: because the frontend serializes visible URLs, PATCH must compare persisted line identity/object hash and restore the persisted equivalent URL when only the active provider representation changed; identity/hash changes remain writable. This avoids JSONB/audit-hash churn without frontend or mass DB updates.
- Task 5 queue ruling: actual states are draft/queued/processing (not running). Keep dual allowlist and Supabase public until all pre-cutover jobs, including stale processing retries, drain; quote input_storage_provider is unrelated to catalog image provider.
- Task 6 implementation ruling: create a standalone script plus one focused test module; do not import the monolithic API or use r2_doctor because its probe creates/deletes objects. Require an externally anchored manifest file SHA-256 plus the exact Task 3 keyset/manifest digest algorithms; dry-run must require no credentials and perform zero network/RPC calls.
- Task 6 extras ruling: the 770 local files absent from the authoritative manifest are `excluded_unmanifested`: enumerate/count/digest them, never open/upload them, and never let them enter the certified keyset. Their presence does not require deletion or prevent certifying the exact 2,214 manifest; missing or mismatched manifest entries remain fatal.
- Task 6 verification ruling: HEAD exact or create-only PUT then HEAD for every entry, followed by a fresh full streaming GET SHA/size/header verification of all 2,214 before any RPC. Verify immutable Cache-Control too; never trust ETag, list/delete/copy/overwrite, or call Supabase Storage bodies.
- Task 6 registry ruling: only after all GETs pass, execute begin, 2,214 add, 2,214 register(provider=r2), finalize using one deterministic/resumable batch; a failure never finalizes. Checkpoint/report are atomic, sanitized, retained, and bound to the manifest anchor/digests; final certification also depends on Task 3 being live and a frozen sync so no existing Supabase registry row conflicts.
- Task 7 sequence correction: split into Gate 7A (private bucket/location/credentials/isolation) → Gate 6 live → Gate 7B (custom domain/CORS/cache/metrics). Never enable r2.dev; it is independent of custom-domain cache and unnecessary for staging.
- Task 7 credential ruling: exact-action temporary R2 credentials use a session token, so API/worker/migrator/preflight/provision need `CATALOG_ASSET_R2_SESSION_TOKEN` before live execute. Runtime HEAD must also require immutable Cache-Control, matching the migrator. Bucket Lock is considered only after first-load correctness because it blocks remediation.
- Task 7 cache/metrics ruling: Smart Tiered is zone-wide and requires explicit authorization; 404/410 TTL 0 and 5xx no-store live in the cache ruleset, with 410/5xx structurally verified unless a Worker is added. Native budget alerts are account-wide USD/pay-as-you-go; 70/85/95% free-tier thresholds and Free-plan HIT-rate acceptance require an external/synthetic monitor.
- Task 7 known public origin is `https://web-lemon-one-45.vercel.app`; no custom asset hostname, zone, plan, location/jurisdiction or USD threshold is inferable safely. These remain explicit user/external inputs; quote-files is out of scope.

Task 0: complete (read-only repository/live preflight; external domain, freeze and metrics gates explicitly deferred to Tasks 7-9; no commit)

Task 1: complete (commits 286228530e493cdd285139875a7b79e4dbf7cb25 + b5be2e2cffddcd9ad58ca0c9c54b472d07fef2c0; RED=6 behavior failures plus one controlled browser mutation; GREEN=70 search/UI + 298 API + 47 UI + 2 Playwright acceptance tests; py_compile, diff-check and API byte parity passed; scoped re-review approved with no findings)

Task 2: complete (commits d7481e1f25a909b58e6534a0c56057b8b9db0e35 + 1921c4af1ed3e493e8534d9039bdc396975d5e78; REST contract corrected to x-metadata Base64 + object/info; previous matching/missing/incompatible assets use True/None/False with 404 restoring only the missing key; no_changes performs no Storage access; final 273 tests, py_compile and scoped diff-check passed; re-review approved, one non-blocking report-count inconsistency recorded)

Task 3: local code complete (commits f4fce31, e42a297, ff94cdb, cdac626, 682c1ba, 3a1f7b2, 3c49e6a; immutable/private manifest entries, begin/add/finalize RPCs, exact digest+registry joins, concurrent ON CONFLICT registration, fully qualified SECURITY DEFINER bodies, real A/bootstrap and B/bootstrap parity; 45 migration tests passed/2 PostgreSQL opt-in skipped, 61 repository, 28 focused API, py_compile/API parity/diff-check passed; re-review approved with one minor partial-env skip diagnostic; live 2,214 backfill/RPC gate remains Tasks 6/8)

Task 4: complete and independently approved (commits 6612299ef05f44488661bf204d945ddd3a07989f + b4141ffada1ba22315e8f171f1316d31e411ccbc; dedicated catalog R2 provider with isolated credentials/client/bucket, create-only PUT plus exact HEAD verification, provider-aware URL/cache fingerprint, nonsecret readiness, dual exact-host allowlist, and environment/preflight/provision wiring; review fixes make provider/base/R2 validation unconditional when sync is paused, require catalog_asset_ready during deploy, provision SUPABASE_SERVICE_KEY safely, and restore the mixed-workbook regression with valid hash keys; re-review found no critical/important/minor issues; 205 combined review tests plus 60 deploy and 37 mixed-workbook tests passed; no live Cloudflare/Supabase mutation and Gate 4 remains pending exact domain/token/bucket/CORS/cache validation)

Task 5: complete and independently approved (commits 56e8ca402099c7ce1b8c0ef2fff7ebe63583ffaf + 0c3c933 + 75e89ace855a7ab60e386ff62a653b57423b38fa; exact bidirectional Supabase/R2 rewrite on deep-copied generic catalog lines, GET private/no-store, provider and general validated no-op PATCH anti-churn, new quote active URL with historical project_context preserved, no frontend or mass JSONB changes, strict URL-edge negatives and triple API parity; re-review found no critical/important/minor issues; 934 relevant tests passed/2 opt-in skipped and final 122 review tests passed; five acceptance failures were independently confirmed as pre-existing W/X-to-AA/Fletes drift outside Task 5)

Task 6: complete and independently approved (commits fdd6ae6f19a6eb369aa8994f937c88ec63a72aa6 + c9f09c45a7b2b5544922500888a9c4da450c977e + 63032947a6f90931f7f5e76a69d48d90b5bffdca; checkpoint is never DB authority, authenticated exact batch SELECT/finalize/reselect proves certification, post-finalize crash is recoverable, PUT consumes immutable revalidated bytes, botocore retries/digest pins/body close/duplicate JSON/root reparse/output sanitization hardened; 187 review tests passed/3 expected skips; final review no issues after doc cleanup). External manifest SHA be3edd24a3753af7d01a70b9da213719a8d0708f2597010792744bc2dc4578ff; original dry-run report SHA bbfe76b3fd3a7086ded638ab3f584df0d699e494a51bdc55ad32ba376868d643; after-review dry-run report SHA 11bd2d51f2f99397091868c4ef3208b8b53a7113c4dc07217185334d624aceed; both passed 2,214/678,858,152 exact, 770/105,398,074 extras excluded, 0 network/RPC. Gate 6 live still blocked by Task3 live/freeze and Cloudflare Gate7A.

Task 7 repository readiness: complete and independently approved (commits 7bd5bc96e96f5d1edfd8bff7f578bdf7d555e5bd + b632a19fbfc206038cb267e7a0a1854d3174f5e0; optional catalog-only session token, immutable Cache-Control HEAD/412 gate, no quote/default/health leakage, PowerShell pre-write rejection of controls/whitespace/>16,384 preventing env injection; 832 initial tests/3 skips plus 67 independent Hetzner re-review tests passed, parser/parity/diff-check clean, no findings). External Gate7A/7B remains blocked on exact hostname/zone/plan/location/budget and separate Smart Tiered/live authority; no external action executed.

Final integration fix loop: in progress after whole-branch review. Required fixes: provider-neutral verified registry consumption for post-cutover Supabase rollback while retaining strict pinned initial R2 batch; pin migration B to batch `470442fc-3dc3-5948-b0e4-1dd34c1fcd30` plus known digests; make SQL runner explicitly distinguish new-DB bootstrap from existing-project A→Gate6→B and document all server-side Vercel catalog env; update four Task1-era supplier/search tests and five certified template-expectation drifts so the full suite can be green. No external gate is authorized or executed.

Final integration fix loop: repository changes complete in `ab59f75` + `64b1898` + `85fc3b5`; provider-neutral idempotent registry and clones preserve strict Task6 R2 certification, migration B is pinned to the exact batch/digests, the SQL runner enforces new-project bootstrap versus existing-project A → Gate7A/Gate6 → B, all catalog Vercel env names are documented, and supplier/acceptance tests match the explicit-search/v17 contract without engine/template changes. Focused verification: 615 passed/3 opt-in skipped/2 warnings; full regression: 4004 passed/26 skipped/127 warnings with 22 failures and 2 setup errors classified as pre-existing template/UI/mirror/fixture debt outside this fix. Gate 8 live was not executed and remains pending.

Independent follow-up review of `88a0ece`: Changes Required without Critical because runner policy used only resolved paths. Fixed in `96641ac` with pure content classification plus known-path identity, one-read-before-connection execution, combined-selection A+B detection, copied bootstrap/B enforcement and fail-closed rejection of altered B pins while preserving unrelated arbitrary SQL. TDD RED 3 failed/9 passed → GREEN 12 passed; hardening RED 1 failed/12 passed → final runner/migrations 130 passed/3 PostgreSQL-Docker opt-in skipped. `py_compile` and scoped diff-check passed. No live gate, DDL, DB connection, deploy, push, engine or template change.

Second independent follow-up of `e50e387`: Changes Required without Critical because substring sentinels were whitespace/comment evadable, comments could satisfy pins, certification guards were incomplete and known paths trusted modified content. Fixed in `8ebb5c4` with a deterministic SQL lexer and length-prefixed token fingerprints for the complete canonical bootstrap/A/B streams; quoted strings remain opaque, comments/BOM/layout are ignored, B-like clone/update structure that is not canonical becomes `cutover_unpinned`, and known paths require canonical content. RED 6 failed/13 passed plus structural RED 1 failed/19 passed; exact final command `python -B -m pytest -p no:cacheprovider -q tests/test_apply_supabase_sql.py tests/test_catalog_migrations.py tests/test_catalog_asset_r2_migration.py tests/test_project_migrations.py` → 138 passed/3 skipped in 5.88s. `py_compile` and scoped diff-check exit 0. Final report commit is identified as `este commit (HEAD en el handoff)`. No live gate, DDL, DB connection, deploy, push, engine or template change.
